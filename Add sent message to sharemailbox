try {
    Set-Mailbox -Identity "email@domain.com" `
        -MessageCopyForSentAsEnabled $true `
        -MessageCopyForSendOnBehalfEnabled $true
    Write-Host "Successful"
} catch {
    Write-Host "Could not process"
}
